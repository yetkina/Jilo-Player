# Jilo Player — public distribution repo (closed-source)
# Ignore everything by default; only marketing files are tracked.
# Binaries (.app, .zip) → GitHub Releases modülü (deploy_mac.sh), repoya commit edilmez.

/*

!.gitignore
!README.md
!CHANGELOG.md
!screenshots/
!screenshots/**

screenshots/**/.DS_Store
screenshots/**/Thumbs.db
